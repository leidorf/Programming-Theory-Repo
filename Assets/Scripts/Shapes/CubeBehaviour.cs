using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CubeBehaviour : ShapesBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        shapeName = "Cube";
    }
}
